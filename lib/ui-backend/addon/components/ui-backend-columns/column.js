import Ember from 'ember';
import layout from '../../templates/components/ui-backend-columns/column';

export default Ember.Component.extend({
  classNameBindings: [ ':ui-backend-columns-column' ],
  layout
});
