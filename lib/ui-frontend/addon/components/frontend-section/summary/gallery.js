import Ember from 'ember';
import layout from '../../../templates/components/frontend-section/summary/gallery';

export default Ember.Component.extend({
  classNameBindings: [ ':gallery' ],
  layout
});
