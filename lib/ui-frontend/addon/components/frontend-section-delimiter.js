import Ember from 'ember';
import layout from '../templates/components/frontend-section-delimiter';

export default Ember.Component.extend({
  classNameBindings: [ ':frontend-section-delimiter' ],
  layout
});
