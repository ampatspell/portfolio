import Base from './frontend-section-base';
import layout from '../templates/components/frontend-section-text';

export default Base.extend({
  classNameBindings: [ ':frontend-section-text' ],
  layout
});
