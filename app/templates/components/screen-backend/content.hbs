{{#ui-backend-master-detail as |md|}}

  {{#md.master}}

    {{ui-backend-header title='Navigation'}}

    {{#ui-backend-tree new=(action (route-action 'new')) as |tree|}}
      {{#each sections as |item|}}
        {{tree.item title=item.title_ description=item.modelScreenName action=(route-action 'select' item)}}
      {{/each}}
    {{/ui-backend-tree}}

  {{/md.master}}

  {{#md.detail}}
    {{outlet}}
  {{/md.detail}}

{{/ui-backend-master-detail}}
