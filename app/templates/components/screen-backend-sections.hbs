{{#ui-backend-master-detail as |md|}}

  {{#md.master}}

    {{ui-backend-header title='Navigation'}}

    {{#ui-backend-tree new=(action (route-action 'new')) array=roots arrayKey='sections' select=(action (route-action 'select')) as |item|}}
      <div class="title">{{item.title_}}</div>
      <div class="info">{{item.modelScreenName}}</div>
    {{/ui-backend-tree}}

  {{/md.master}}

  {{#md.detail}}
    {{outlet}}
  {{/md.detail}}

{{/ui-backend-master-detail}}
