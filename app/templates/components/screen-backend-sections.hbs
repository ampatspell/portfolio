{{#ui-backend-master-detail as |md|}}

  {{#md.master}}

    {{ui-backend-header title='Navigation'}}

    {{#ui-backend-tree new=(action (route-action 'newSection')) array=roots arrayKey='sections' select=(action (route-action 'selectSection')) as |item|}}
      <div class="title">{{item.title_}}</div>
      <div class="info">{{item.modelScreenName}}</div>
    {{/ui-backend-tree}}

  {{/md.master}}

  {{#md.detail}}
    {{outlet}}
  {{/md.detail}}

{{/ui-backend-master-detail}}
