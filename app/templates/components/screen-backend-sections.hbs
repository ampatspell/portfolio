{{#ui-backend-master-detail as |md|}}

  {{#md.master}}

    {{ui-backend-header title='Navigation'}}

    {{#ui-backend-tree
        array=roots
        arrayKey='sections'
        sort=(action 'sort')
        new=(action (route-action 'newSection'))
        select=(action (route-action 'selectSection')) as |item|}}

      <div class="title">{{item.title_}}</div>
      <div class="info">{{item.modelScreenName}}</div>

    {{/ui-backend-tree}}

  {{/md.master}}

  {{#md.detail}}
    {{outlet}}
  {{/md.detail}}

{{/ui-backend-master-detail}}
